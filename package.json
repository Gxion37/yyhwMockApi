{
  "name": "json-server-test",
  "main": "server.js",
  "scripts": {
    "createNewData": "node app.js",
    "start": "node app.js & json-server --watch db.json",
    "start:Server": "json-server --watch db.json"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.5.0",
    "json-server": "^0.14.0"
  }
}
